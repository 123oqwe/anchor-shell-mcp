{
  "name": "@anchor/shell-mcp",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Safe shell-exec MCP server. Token-efficient alternative to wrapping every CLI in its own MCP — agent generates command strings, server runs them with deny-list + timeout + output cap.",
  "bin": { "anchor-shell-mcp": "dist/index.js" },
  "main": "dist/index.js",
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx --watch src/index.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
